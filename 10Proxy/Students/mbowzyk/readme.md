Bardzo prosty przykład proxy, jako pośrednika. 

Dwie klasy implementują interfejs. Pierwsza, `DataLoader`, pozwala dostać wyniki zapytana bezpośrednio, druga, `SecureDataLoader`, by je otrzymać, pyta się pierwszej.